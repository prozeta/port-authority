<%
endpoints = scope['portauthority::cluster_members'].empty? ? ['localhost'] : scope['portauthority::cluster_members']
-%>
---
:debug: <%= scope['portauthority::debug'] %>
:syslog: true
:etcd:
  :endpoints:
<% endpoints.each do |e| -%>
    - http://<%= e %>:2379
<% end -%>
  :timeout: 2
